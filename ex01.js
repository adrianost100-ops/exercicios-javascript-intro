// Declaração das variáveis
let protocolo = 12345;
let cliente = "Adriano";
let produto = "Notebook";
let tempoEsperaMinutos = 30;

// Saída formatada
console.log(`Olá ${cliente}, seu chamado de número #${protocolo} para o produto ${produto} foi registrado. Tempo estimado: ${tempoEsperaMinutos} min.`);
